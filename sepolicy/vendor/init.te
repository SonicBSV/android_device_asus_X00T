type_transition init nvram_btwifi_exec:process nvram_btwifi;
dontaudit init nvram_btwifi:process noatsecure;
dontaudit init domain:process noatsecure;
allow init hwservicemanager:binder { call transfer };
allow init btnvtool_exec:file execute;
allow init ipa_dev:chr_file open;
allow init ion_device:chr_file ioctl;
allow init proc:file { getattr open setattr read };
allow init property_socket:sock_file write;
allow init sysfs:file { open read write };
allow init sysfs_dm:file { open write };
allow init tee_device:chr_file { write ioctl };
allow init vndbinder_device:chr_file { ioctl open read write };
allow init vendor_file:file { execute execute_no_trans };
allow init cameraserver:process noatsecure;
allow init hal_camera_default:process noatsecure;
allow init hal_wifi_supplicant_default:process noatsecure;
allow init adbd:process noatsecure;
allow init audioserver:process noatsecure;
allow init fsck:process noatsecure;
allow init hal_audio_default:process noatsecure;
allow init hal_bootctl_default:process noatsecure;
allow init hal_drm_hwservice:hwservice_manager add;
allow init hal_gatekeeper_qti:process noatsecure;
allow init hal_keymaster_qti:process noatsecure;
allow init hidl_base_hwservice:hwservice_manager add;
allow init mediaserver:process noatsecure;
allow init qti_init_shell:process noatsecure;
allow init recovery_refresh:process noatsecure;
allow init tee:process noatsecure;
allow init ueventd:process noatsecure;
allow init vendor_init:process noatsecure;
allow init vold:process noatsecure;
allow init bootanim:process noatsecure;
allow init hal_cas_default:process noatsecure;
allow init hal_configstore_default:process noatsecure;
allow init hal_drm_default:process noatsecure;
allow init healthd:process noatsecure;
allow init hwservicemanager:process noatsecure;
allow init ipacm:process noatsecure;
allow init netd:process noatsecure;
allow init recovery_persist:process noatsecure;
allow init rild:process noatsecure;
allow init servicemanager:process noatsecure;
allow init tzdatacheck:process noatsecure;
allow init update_verifier:process noatsecure;
allow init vdc:process noatsecure;
allow init vndservicemanager:process noatsecure;
allow init zygote:process noatsecure;
allow init hal_drm_clearkey:process noatsecure;
allow init hal_graphics_allocator_default:process noatsecure;
allow init hal_graphics_composer_default:process noatsecure;
allow init hal_health_default:process noatsecure;
allow init hal_ir_default:process noatsecure;
allow init hal_sensors_default:process noatsecure;
allow init hal_usb_default:process noatsecure;
allow init msm_irqbalanced:process noatsecure;
allow init netmgrd:process noatsecure;
allow init port-bridge:process noatsecure;
allow init qti:process noatsecure;
allow init adsprpcd:process noatsecure;
allow init hal_display_color_default:process noatsecure;
allow init hal_dpmQmiMgr:process noatsecure;
allow init hal_light_default:process noatsecure;
allow init hal_memtrack_default:process noatsecure;
allow init hal_power_default:process noatsecure;
allow init hal_thermal_default:process noatsecure;
allow init hal_vibrator_default:process noatsecure;
allow init hal_wifi_default:process noatsecure;
allow init hal_wifi_hostapd_default:process noatsecure;
allow init mm-pp-daemon:process noatsecure;
allow init vendor_hal_factory_qti_default:process noatsecure;
allow init vendor_per_mgr:process noatsecure;
allow init drmserver:process noatsecure;
allow init hal_alarm_qti_default:process noatsecure;
allow init hal_esepowermanager_qti:process noatsecure;
allow init hal_gnss_qti:process noatsecure;
allow init hal_perf_default:process noatsecure;
allow init hal_perf_default:binder call;
allow init hal_perf_hwservice:hwservice_manager find;
allow init hwservicemanager:binder call;
allow init mediadrmserver:binder transfer;
allow init self:process execmem;
allow init tee_device:chr_file write;
allow init hal_qdutils_disp_qti:process noatsecure;
allow init hal_qteeconnector_qti:process noatsecure;
allow init hal_tui_comm_qti:process noatsecure;
allow init installd:process noatsecure;
allow init mediacodec:process noatsecure;
allow init mediadrmserver:process noatsecure;
allow init mediaextractor:process noatsecure;
allow init irsc_util:process noatsecure;
allow init rfs_access:process noatsecure;
allow init rmt_storage:process noatsecure;
allow init self:process execmem;
allow init mediadrmserver:binder call;
allow init sensors:process noatsecure;
allow init mediadrmserver:binder transfer;
allow init atfwd:process noatsecure;
allow init energyawareness:process noatsecure;
allow init hvdcp:process noatsecure;
allow init keystore:process noatsecure;
allow init thermal-engine:process noatsecure;
allow init init_blutmac_exec:file { execute execute_no_trans };
allow init nvram_btwifi:process { rlimitinh siginh transition };
allow init nvram_btwifi_exec:file { read getattr execute map open };
allow init mediametrics:process noatsecure;
allow init mdnsd:process { noatsecure };
allow init sysfs_graphics:file { write };
allow init hal_drm_widevine:process { noatsecure };
allow init hal_nfc_default:process { noatsecure };
allow init sysfs_graphics:file { open };
allow init sysfs_graphics:file { read };
allow init system_file:file { execute_no_trans };
allow init vendor_toolbox_exec:file { execute_no_trans };
allow init vfat:file { getattr };
allow init vfat:file { read };
allow init diag_device:chr_file { getattr setattr open read write };
allow init pmsg_device:chr_file { getattr open read write };
allow init hal_soter_qti:process { noatsecure };
allow init lmkd:process { noatsecure };
allow init qrtr:process { noatsecure };
allow init system_file:dir { relabelfrom };
allow init system_file:file { relabelfrom };
allow init vendor_pd_mapper:process { noatsecure };
allow init toolbox:process { noatsecure };
allow init vendor_ssr_setup:process { noatsecure };
allow init ims:process { noatsecure };
allow init incidentd:process { noatsecure };
allow init time_daemon:process { noatsecure };
allow init wcnss_service:process { noatsecure };
allow init wificond:process { noatsecure };
allow init untrusted_app:fd { use };
allow init untrusted_app_27:fd { use };
allow init mediadrmserver:binder transfer;
allow init diag_device:chr_file ioctl;
allow init shell_exec:file execute_no_trans;
allow init sysfs:file setattr;
allow init asus_partition_file:file setattr;
allow init servicemanager:binder call;
allow init vendor_configs_file:dir relabelfrom;
allow init vendor_configs_file:file relabelfrom;
allow init proc:file write;
allow init sysfs_devices_system_cpu:file write;
allow init sysfs_msm_power:file { write open };
allow init sysfs_thermal:file { write open };
allow init sysfs_lowmemorykiller:file { read open write };
allow init sysfs_process_reclaim:file { read open write };
allow init vfat:dir { open read write search };
allow init sysfs_mmc_host:file { read open write };
allow init sysfs_vmpressure:file { read open write } ;
allow init vfat:file { read open write };
allow init sysfs_devfreq:file { read open write };
allow init sysfs_socinfo:file { read open write };
allow init app_data_file:dir { search read write getattr open add_name remove_name create unlink rmdir };
allow init app_data_file:file { read write setattr getattr open create unlink ioctl lock };
allow init hal_power_hwservice:hwservice_manager add;
allow init vendor_configs_file:dir relabelfrom;
allow init vendor_configs_file:file relabelfrom;
allow init self:netlink_socket read;
allow init sysfs_battery_supply:file { setattr open write };
allow init system_server:binder call;
allow init system_server:binder transfer;
allow init package_service:service_manager find;
allow init hal_wifi_offload_default:process noatsecure;
allow init init-qti-fbe-sh:process noatsecure;
allow init move-time-data-sh:process noatsecure;
#allow init hal_light_hwservice:hwservice_manager add;
#allow init sysfs_io_sched_tuneable:file { setattr w_file_perms };
allow init system_file:file mounton;
allow init hal_power_stats_hwservice:hwservice_manager add;
allow init persist_block_device:lnk_file { relabelto read write setattr getattr open };
allow init sysfs_leds:lnk_file { r_file_perms };
allow init privapp_data_file:dir { add_name rmdir search getattr open read remove_name write };
allow init privapp_data_file:file { setattr getattr ioctl lock open read unlink write };
allow init oemfs:dir mounton;
allow init same_process_hal_file:file { execute r_file_perms };;
allow init hal_thermal_hwservice:hwservice_manager add;
allow init hal_codec2_hwservice:hwservice_manager find;
allow init hal_omx_hwservice:hwservice_manager find;
allow init hidl_manager_hwservice:hwservice_manager find;
allow init mediacodec:binder { call transfer };
allow init mediametrics:binder call;
allow init mediametrics_service:service_manager find;
allow init mediaswcodec:binder call;
allow init nfc:binder { call transfer };
allow init platform_app:binder { call transfer };
allow init proc_touchpanel:file write;
allow init qdisplay_service:service_manager find;
allow init ion_device:chr_file { ioctl open read write };
allow init qdsp_device:chr_file { ioctl open read write };
allow init vendor_data_file:file lock;
allow init hal_audio_default:binder call;
allow init platform_app:binder call;
